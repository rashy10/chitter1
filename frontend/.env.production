# Profile: PRODUCTION  —  loaded by `npm run build` (Vite's default build mode)
#
# This is what ships to Vercel. A VITE_BACKEND_BASE set in the Vercel dashboard
# takes precedence over this file, so the dashboard remains the source of truth for
# the real deploy; this value is the committed fallback.
VITE_BACKEND_BASE=https://chitter-backend-app-4c5e1318fbab.herokuapp.com
