{
  "name": "chitter1-root",
  "private": true,
  "scripts": {
    "start": "node backend/index.js",
    "heroku-postbuild": "npm install --prefix backend && npm install --prefix frontend --production=false && npm run build --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "dev:web:remote": "npm run dev:remote --prefix frontend",
    "build:web": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  },
  "dependencies": {
    "react-router-dom": "^7.9.4"
  }
}
